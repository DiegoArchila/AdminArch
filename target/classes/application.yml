spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adminarch
    username: postgres
    password: maresofi
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update